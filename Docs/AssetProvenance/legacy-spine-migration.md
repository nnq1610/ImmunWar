# Legacy Spine migration

The Spine 3.7.94 source project, loose source images, exported skeleton JSON, and atlas text were moved from Unity runtime import scope to `AssetSource/ApprovedMasters/LegacySpine` on 2026-09-18.

`Assets/ImmunWar/Art/Enemies/AnimatedVirus/coronavirus.png` remains only as a candidate static fallback. It is not release-approved until its provenance record, export hash, and rights evidence pass validation. The project does not add the Spine runtime or a custom parser.
